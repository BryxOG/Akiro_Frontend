document.addEventListener('DOMContentLoaded', (event) => {
    const cabinetContainer = document.querySelector('.main-container');
    if (cabinetContainer) {
        cabinetContainer.classList.add('slide-in');
    }
});